---
version: 0.2

phases:
    install:
        runtime-versions:
            nodejs: 16
        commands:
            - npm i
    build:
        commands:
            - echo 'Building'
            - npm run build
artifacts:
    files:
        - ./**/*
    base-directory: 'dist'
