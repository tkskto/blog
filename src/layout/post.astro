---
import BlogLayout from './blog.astro';
import TheArticle from '../component/TheArticle.astro';

const {title, publishDate} = Astro.props.frontmatter;
---
<BlogLayout pageTitle={title}>
    <TheArticle level="1" title={title} date={publishDate}>
        <slot />
    </TheArticle>
</BlogLayout>
